// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/motifpath/core-domain/internal/adapters/repo/ent/pathassignment"
)

// PathAssignment is the model entity for the PathAssignment schema.
type PathAssignment struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// StudentID holds the value of the "student_id" field.
	StudentID uuid.UUID `json:"student_id,omitempty"`
	// LearningPathID holds the value of the "learning_path_id" field.
	LearningPathID uuid.UUID `json:"learning_path_id,omitempty"`
	// AssignedBy holds the value of the "assigned_by" field.
	AssignedBy uuid.UUID `json:"assigned_by,omitempty"`
	// AssignedAt holds the value of the "assigned_at" field.
	AssignedAt   time.Time `json:"assigned_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PathAssignment) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case pathassignment.FieldAssignedAt:
			values[i] = new(sql.NullTime)
		case pathassignment.FieldID, pathassignment.FieldStudentID, pathassignment.FieldLearningPathID, pathassignment.FieldAssignedBy:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PathAssignment fields.
func (_m *PathAssignment) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case pathassignment.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case pathassignment.FieldStudentID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field student_id", values[i])
			} else if value != nil {
				_m.StudentID = *value
			}
		case pathassignment.FieldLearningPathID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field learning_path_id", values[i])
			} else if value != nil {
				_m.LearningPathID = *value
			}
		case pathassignment.FieldAssignedBy:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field assigned_by", values[i])
			} else if value != nil {
				_m.AssignedBy = *value
			}
		case pathassignment.FieldAssignedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field assigned_at", values[i])
			} else if value.Valid {
				_m.AssignedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PathAssignment.
// This includes values selected through modifiers, order, etc.
func (_m *PathAssignment) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this PathAssignment.
// Note that you need to call PathAssignment.Unwrap() before calling this method if this PathAssignment
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *PathAssignment) Update() *PathAssignmentUpdateOne {
	return NewPathAssignmentClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the PathAssignment entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *PathAssignment) Unwrap() *PathAssignment {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: PathAssignment is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *PathAssignment) String() string {
	var builder strings.Builder
	builder.WriteString("PathAssignment(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("student_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.StudentID))
	builder.WriteString(", ")
	builder.WriteString("learning_path_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.LearningPathID))
	builder.WriteString(", ")
	builder.WriteString("assigned_by=")
	builder.WriteString(fmt.Sprintf("%v", _m.AssignedBy))
	builder.WriteString(", ")
	builder.WriteString("assigned_at=")
	builder.WriteString(_m.AssignedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// PathAssignments is a parsable slice of PathAssignment.
type PathAssignments []*PathAssignment
