// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/motifpath/core-domain/internal/adapters/repo/ent/diagram"
	"github.com/motifpath/core-domain/internal/adapters/repo/ent/diagramskill"
	"github.com/motifpath/core-domain/internal/adapters/repo/ent/skill"
)

// DiagramSkillCreate is the builder for creating a DiagramSkill entity.
type DiagramSkillCreate struct {
	config
	mutation *DiagramSkillMutation
	hooks    []Hook
}

// SetDiagramID sets the "diagram_id" field.
func (_c *DiagramSkillCreate) SetDiagramID(v uuid.UUID) *DiagramSkillCreate {
	_c.mutation.SetDiagramID(v)
	return _c
}

// SetSkillID sets the "skill_id" field.
func (_c *DiagramSkillCreate) SetSkillID(v uuid.UUID) *DiagramSkillCreate {
	_c.mutation.SetSkillID(v)
	return _c
}

// SetLinkedAt sets the "linked_at" field.
func (_c *DiagramSkillCreate) SetLinkedAt(v time.Time) *DiagramSkillCreate {
	_c.mutation.SetLinkedAt(v)
	return _c
}

// SetNillableLinkedAt sets the "linked_at" field if the given value is not nil.
func (_c *DiagramSkillCreate) SetNillableLinkedAt(v *time.Time) *DiagramSkillCreate {
	if v != nil {
		_c.SetLinkedAt(*v)
	}
	return _c
}

// SetDiagram sets the "diagram" edge to the Diagram entity.
func (_c *DiagramSkillCreate) SetDiagram(v *Diagram) *DiagramSkillCreate {
	return _c.SetDiagramID(v.ID)
}

// SetSkill sets the "skill" edge to the Skill entity.
func (_c *DiagramSkillCreate) SetSkill(v *Skill) *DiagramSkillCreate {
	return _c.SetSkillID(v.ID)
}

// Mutation returns the DiagramSkillMutation object of the builder.
func (_c *DiagramSkillCreate) Mutation() *DiagramSkillMutation {
	return _c.mutation
}

// Save creates the DiagramSkill in the database.
func (_c *DiagramSkillCreate) Save(ctx context.Context) (*DiagramSkill, error) {
	_c.defaults()
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *DiagramSkillCreate) SaveX(ctx context.Context) *DiagramSkill {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *DiagramSkillCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *DiagramSkillCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (_c *DiagramSkillCreate) defaults() {
	if _, ok := _c.mutation.LinkedAt(); !ok {
		v := diagramskill.DefaultLinkedAt()
		_c.mutation.SetLinkedAt(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *DiagramSkillCreate) check() error {
	if _, ok := _c.mutation.DiagramID(); !ok {
		return &ValidationError{Name: "diagram_id", err: errors.New(`ent: missing required field "DiagramSkill.diagram_id"`)}
	}
	if _, ok := _c.mutation.SkillID(); !ok {
		return &ValidationError{Name: "skill_id", err: errors.New(`ent: missing required field "DiagramSkill.skill_id"`)}
	}
	if _, ok := _c.mutation.LinkedAt(); !ok {
		return &ValidationError{Name: "linked_at", err: errors.New(`ent: missing required field "DiagramSkill.linked_at"`)}
	}
	if len(_c.mutation.DiagramIDs()) == 0 {
		return &ValidationError{Name: "diagram", err: errors.New(`ent: missing required edge "DiagramSkill.diagram"`)}
	}
	if len(_c.mutation.SkillIDs()) == 0 {
		return &ValidationError{Name: "skill", err: errors.New(`ent: missing required edge "DiagramSkill.skill"`)}
	}
	return nil
}

func (_c *DiagramSkillCreate) sqlSave(ctx context.Context) (*DiagramSkill, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *DiagramSkillCreate) createSpec() (*DiagramSkill, *sqlgraph.CreateSpec) {
	var (
		_node = &DiagramSkill{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(diagramskill.Table, sqlgraph.NewFieldSpec(diagramskill.FieldID, field.TypeInt))
	)
	if value, ok := _c.mutation.LinkedAt(); ok {
		_spec.SetField(diagramskill.FieldLinkedAt, field.TypeTime, value)
		_node.LinkedAt = value
	}
	if nodes := _c.mutation.DiagramIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   diagramskill.DiagramTable,
			Columns: []string{diagramskill.DiagramColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(diagram.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.DiagramID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := _c.mutation.SkillIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   diagramskill.SkillTable,
			Columns: []string{diagramskill.SkillColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(skill.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.SkillID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// DiagramSkillCreateBulk is the builder for creating many DiagramSkill entities in bulk.
type DiagramSkillCreateBulk struct {
	config
	err      error
	builders []*DiagramSkillCreate
}

// Save creates the DiagramSkill entities in the database.
func (_c *DiagramSkillCreateBulk) Save(ctx context.Context) ([]*DiagramSkill, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*DiagramSkill, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*DiagramSkillMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *DiagramSkillCreateBulk) SaveX(ctx context.Context) []*DiagramSkill {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *DiagramSkillCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *DiagramSkillCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}
