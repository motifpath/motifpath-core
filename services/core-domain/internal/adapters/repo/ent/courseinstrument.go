// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/motifpath/core-domain/internal/adapters/repo/ent/course"
	"github.com/motifpath/core-domain/internal/adapters/repo/ent/courseinstrument"
	"github.com/motifpath/core-domain/internal/adapters/repo/ent/instrument"
)

// CourseInstrument is the model entity for the CourseInstrument schema.
type CourseInstrument struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CourseID holds the value of the "course_id" field.
	CourseID uuid.UUID `json:"course_id,omitempty"`
	// InstrumentID holds the value of the "instrument_id" field.
	InstrumentID uuid.UUID `json:"instrument_id,omitempty"`
	// LinkedAt holds the value of the "linked_at" field.
	LinkedAt time.Time `json:"linked_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CourseInstrumentQuery when eager-loading is set.
	Edges        CourseInstrumentEdges `json:"edges"`
	selectValues sql.SelectValues
}

// CourseInstrumentEdges holds the relations/edges for other nodes in the graph.
type CourseInstrumentEdges struct {
	// Course holds the value of the course edge.
	Course *Course `json:"course,omitempty"`
	// Instrument holds the value of the instrument edge.
	Instrument *Instrument `json:"instrument,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// CourseOrErr returns the Course value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CourseInstrumentEdges) CourseOrErr() (*Course, error) {
	if e.Course != nil {
		return e.Course, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: course.Label}
	}
	return nil, &NotLoadedError{edge: "course"}
}

// InstrumentOrErr returns the Instrument value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CourseInstrumentEdges) InstrumentOrErr() (*Instrument, error) {
	if e.Instrument != nil {
		return e.Instrument, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: instrument.Label}
	}
	return nil, &NotLoadedError{edge: "instrument"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CourseInstrument) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case courseinstrument.FieldID:
			values[i] = new(sql.NullInt64)
		case courseinstrument.FieldLinkedAt:
			values[i] = new(sql.NullTime)
		case courseinstrument.FieldCourseID, courseinstrument.FieldInstrumentID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CourseInstrument fields.
func (_m *CourseInstrument) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case courseinstrument.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case courseinstrument.FieldCourseID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field course_id", values[i])
			} else if value != nil {
				_m.CourseID = *value
			}
		case courseinstrument.FieldInstrumentID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field instrument_id", values[i])
			} else if value != nil {
				_m.InstrumentID = *value
			}
		case courseinstrument.FieldLinkedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field linked_at", values[i])
			} else if value.Valid {
				_m.LinkedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CourseInstrument.
// This includes values selected through modifiers, order, etc.
func (_m *CourseInstrument) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCourse queries the "course" edge of the CourseInstrument entity.
func (_m *CourseInstrument) QueryCourse() *CourseQuery {
	return NewCourseInstrumentClient(_m.config).QueryCourse(_m)
}

// QueryInstrument queries the "instrument" edge of the CourseInstrument entity.
func (_m *CourseInstrument) QueryInstrument() *InstrumentQuery {
	return NewCourseInstrumentClient(_m.config).QueryInstrument(_m)
}

// Update returns a builder for updating this CourseInstrument.
// Note that you need to call CourseInstrument.Unwrap() before calling this method if this CourseInstrument
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *CourseInstrument) Update() *CourseInstrumentUpdateOne {
	return NewCourseInstrumentClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the CourseInstrument entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *CourseInstrument) Unwrap() *CourseInstrument {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: CourseInstrument is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *CourseInstrument) String() string {
	var builder strings.Builder
	builder.WriteString("CourseInstrument(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("course_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.CourseID))
	builder.WriteString(", ")
	builder.WriteString("instrument_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.InstrumentID))
	builder.WriteString(", ")
	builder.WriteString("linked_at=")
	builder.WriteString(_m.LinkedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// CourseInstruments is a parsable slice of CourseInstrument.
type CourseInstruments []*CourseInstrument
