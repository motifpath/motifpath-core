// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/motifpath/core-domain/internal/adapters/repo/ent/contentnode"
	"github.com/motifpath/core-domain/internal/adapters/repo/ent/contentnodeexercise"
	"github.com/motifpath/core-domain/internal/adapters/repo/ent/exercise"
	"github.com/motifpath/core-domain/internal/adapters/repo/ent/predicate"
)

// ContentNodeExerciseQuery is the builder for querying ContentNodeExercise entities.
type ContentNodeExerciseQuery struct {
	config
	ctx             *QueryContext
	order           []contentnodeexercise.OrderOption
	inters          []Interceptor
	predicates      []predicate.ContentNodeExercise
	withContentNode *ContentNodeQuery
	withExercise    *ExerciseQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ContentNodeExerciseQuery builder.
func (_q *ContentNodeExerciseQuery) Where(ps ...predicate.ContentNodeExercise) *ContentNodeExerciseQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ContentNodeExerciseQuery) Limit(limit int) *ContentNodeExerciseQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ContentNodeExerciseQuery) Offset(offset int) *ContentNodeExerciseQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ContentNodeExerciseQuery) Unique(unique bool) *ContentNodeExerciseQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ContentNodeExerciseQuery) Order(o ...contentnodeexercise.OrderOption) *ContentNodeExerciseQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryContentNode chains the current query on the "content_node" edge.
func (_q *ContentNodeExerciseQuery) QueryContentNode() *ContentNodeQuery {
	query := (&ContentNodeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(contentnodeexercise.Table, contentnodeexercise.FieldID, selector),
			sqlgraph.To(contentnode.Table, contentnode.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, contentnodeexercise.ContentNodeTable, contentnodeexercise.ContentNodeColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryExercise chains the current query on the "exercise" edge.
func (_q *ContentNodeExerciseQuery) QueryExercise() *ExerciseQuery {
	query := (&ExerciseClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(contentnodeexercise.Table, contentnodeexercise.FieldID, selector),
			sqlgraph.To(exercise.Table, exercise.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, contentnodeexercise.ExerciseTable, contentnodeexercise.ExerciseColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ContentNodeExercise entity from the query.
// Returns a *NotFoundError when no ContentNodeExercise was found.
func (_q *ContentNodeExerciseQuery) First(ctx context.Context) (*ContentNodeExercise, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{contentnodeexercise.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ContentNodeExerciseQuery) FirstX(ctx context.Context) *ContentNodeExercise {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ContentNodeExercise ID from the query.
// Returns a *NotFoundError when no ContentNodeExercise ID was found.
func (_q *ContentNodeExerciseQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{contentnodeexercise.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ContentNodeExerciseQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ContentNodeExercise entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ContentNodeExercise entity is found.
// Returns a *NotFoundError when no ContentNodeExercise entities are found.
func (_q *ContentNodeExerciseQuery) Only(ctx context.Context) (*ContentNodeExercise, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{contentnodeexercise.Label}
	default:
		return nil, &NotSingularError{contentnodeexercise.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ContentNodeExerciseQuery) OnlyX(ctx context.Context) *ContentNodeExercise {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ContentNodeExercise ID in the query.
// Returns a *NotSingularError when more than one ContentNodeExercise ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ContentNodeExerciseQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{contentnodeexercise.Label}
	default:
		err = &NotSingularError{contentnodeexercise.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ContentNodeExerciseQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ContentNodeExercises.
func (_q *ContentNodeExerciseQuery) All(ctx context.Context) ([]*ContentNodeExercise, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ContentNodeExercise, *ContentNodeExerciseQuery]()
	return withInterceptors[[]*ContentNodeExercise](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ContentNodeExerciseQuery) AllX(ctx context.Context) []*ContentNodeExercise {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ContentNodeExercise IDs.
func (_q *ContentNodeExerciseQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(contentnodeexercise.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ContentNodeExerciseQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ContentNodeExerciseQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ContentNodeExerciseQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ContentNodeExerciseQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ContentNodeExerciseQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ContentNodeExerciseQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ContentNodeExerciseQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ContentNodeExerciseQuery) Clone() *ContentNodeExerciseQuery {
	if _q == nil {
		return nil
	}
	return &ContentNodeExerciseQuery{
		config:          _q.config,
		ctx:             _q.ctx.Clone(),
		order:           append([]contentnodeexercise.OrderOption{}, _q.order...),
		inters:          append([]Interceptor{}, _q.inters...),
		predicates:      append([]predicate.ContentNodeExercise{}, _q.predicates...),
		withContentNode: _q.withContentNode.Clone(),
		withExercise:    _q.withExercise.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithContentNode tells the query-builder to eager-load the nodes that are connected to
// the "content_node" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ContentNodeExerciseQuery) WithContentNode(opts ...func(*ContentNodeQuery)) *ContentNodeExerciseQuery {
	query := (&ContentNodeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withContentNode = query
	return _q
}

// WithExercise tells the query-builder to eager-load the nodes that are connected to
// the "exercise" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ContentNodeExerciseQuery) WithExercise(opts ...func(*ExerciseQuery)) *ContentNodeExerciseQuery {
	query := (&ExerciseClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withExercise = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ContentNodeID uuid.UUID `json:"content_node_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ContentNodeExercise.Query().
//		GroupBy(contentnodeexercise.FieldContentNodeID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ContentNodeExerciseQuery) GroupBy(field string, fields ...string) *ContentNodeExerciseGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ContentNodeExerciseGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = contentnodeexercise.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ContentNodeID uuid.UUID `json:"content_node_id,omitempty"`
//	}
//
//	client.ContentNodeExercise.Query().
//		Select(contentnodeexercise.FieldContentNodeID).
//		Scan(ctx, &v)
func (_q *ContentNodeExerciseQuery) Select(fields ...string) *ContentNodeExerciseSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ContentNodeExerciseSelect{ContentNodeExerciseQuery: _q}
	sbuild.label = contentnodeexercise.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ContentNodeExerciseSelect configured with the given aggregations.
func (_q *ContentNodeExerciseQuery) Aggregate(fns ...AggregateFunc) *ContentNodeExerciseSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ContentNodeExerciseQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !contentnodeexercise.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ContentNodeExerciseQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ContentNodeExercise, error) {
	var (
		nodes       = []*ContentNodeExercise{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withContentNode != nil,
			_q.withExercise != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ContentNodeExercise).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ContentNodeExercise{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withContentNode; query != nil {
		if err := _q.loadContentNode(ctx, query, nodes, nil,
			func(n *ContentNodeExercise, e *ContentNode) { n.Edges.ContentNode = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withExercise; query != nil {
		if err := _q.loadExercise(ctx, query, nodes, nil,
			func(n *ContentNodeExercise, e *Exercise) { n.Edges.Exercise = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ContentNodeExerciseQuery) loadContentNode(ctx context.Context, query *ContentNodeQuery, nodes []*ContentNodeExercise, init func(*ContentNodeExercise), assign func(*ContentNodeExercise, *ContentNode)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ContentNodeExercise)
	for i := range nodes {
		fk := nodes[i].ContentNodeID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(contentnode.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "content_node_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ContentNodeExerciseQuery) loadExercise(ctx context.Context, query *ExerciseQuery, nodes []*ContentNodeExercise, init func(*ContentNodeExercise), assign func(*ContentNodeExercise, *Exercise)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ContentNodeExercise)
	for i := range nodes {
		fk := nodes[i].ExerciseID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(exercise.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "exercise_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *ContentNodeExerciseQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ContentNodeExerciseQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(contentnodeexercise.Table, contentnodeexercise.Columns, sqlgraph.NewFieldSpec(contentnodeexercise.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, contentnodeexercise.FieldID)
		for i := range fields {
			if fields[i] != contentnodeexercise.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withContentNode != nil {
			_spec.Node.AddColumnOnce(contentnodeexercise.FieldContentNodeID)
		}
		if _q.withExercise != nil {
			_spec.Node.AddColumnOnce(contentnodeexercise.FieldExerciseID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ContentNodeExerciseQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(contentnodeexercise.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = contentnodeexercise.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ContentNodeExerciseGroupBy is the group-by builder for ContentNodeExercise entities.
type ContentNodeExerciseGroupBy struct {
	selector
	build *ContentNodeExerciseQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ContentNodeExerciseGroupBy) Aggregate(fns ...AggregateFunc) *ContentNodeExerciseGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ContentNodeExerciseGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ContentNodeExerciseQuery, *ContentNodeExerciseGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ContentNodeExerciseGroupBy) sqlScan(ctx context.Context, root *ContentNodeExerciseQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ContentNodeExerciseSelect is the builder for selecting fields of ContentNodeExercise entities.
type ContentNodeExerciseSelect struct {
	*ContentNodeExerciseQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ContentNodeExerciseSelect) Aggregate(fns ...AggregateFunc) *ContentNodeExerciseSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ContentNodeExerciseSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ContentNodeExerciseQuery, *ContentNodeExerciseSelect](ctx, _s.ContentNodeExerciseQuery, _s, _s.inters, v)
}

func (_s *ContentNodeExerciseSelect) sqlScan(ctx context.Context, root *ContentNodeExerciseQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
