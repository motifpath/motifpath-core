// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/motifpath/core-domain/internal/adapters/repo/ent/diagram"
	"github.com/motifpath/core-domain/internal/adapters/repo/ent/diagramskill"
	"github.com/motifpath/core-domain/internal/adapters/repo/ent/skill"
)

// DiagramSkill is the model entity for the DiagramSkill schema.
type DiagramSkill struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// DiagramID holds the value of the "diagram_id" field.
	DiagramID uuid.UUID `json:"diagram_id,omitempty"`
	// SkillID holds the value of the "skill_id" field.
	SkillID uuid.UUID `json:"skill_id,omitempty"`
	// LinkedAt holds the value of the "linked_at" field.
	LinkedAt time.Time `json:"linked_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DiagramSkillQuery when eager-loading is set.
	Edges        DiagramSkillEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DiagramSkillEdges holds the relations/edges for other nodes in the graph.
type DiagramSkillEdges struct {
	// Diagram holds the value of the diagram edge.
	Diagram *Diagram `json:"diagram,omitempty"`
	// Skill holds the value of the skill edge.
	Skill *Skill `json:"skill,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// DiagramOrErr returns the Diagram value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DiagramSkillEdges) DiagramOrErr() (*Diagram, error) {
	if e.Diagram != nil {
		return e.Diagram, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: diagram.Label}
	}
	return nil, &NotLoadedError{edge: "diagram"}
}

// SkillOrErr returns the Skill value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DiagramSkillEdges) SkillOrErr() (*Skill, error) {
	if e.Skill != nil {
		return e.Skill, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: skill.Label}
	}
	return nil, &NotLoadedError{edge: "skill"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DiagramSkill) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case diagramskill.FieldID:
			values[i] = new(sql.NullInt64)
		case diagramskill.FieldLinkedAt:
			values[i] = new(sql.NullTime)
		case diagramskill.FieldDiagramID, diagramskill.FieldSkillID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DiagramSkill fields.
func (_m *DiagramSkill) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case diagramskill.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case diagramskill.FieldDiagramID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field diagram_id", values[i])
			} else if value != nil {
				_m.DiagramID = *value
			}
		case diagramskill.FieldSkillID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field skill_id", values[i])
			} else if value != nil {
				_m.SkillID = *value
			}
		case diagramskill.FieldLinkedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field linked_at", values[i])
			} else if value.Valid {
				_m.LinkedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DiagramSkill.
// This includes values selected through modifiers, order, etc.
func (_m *DiagramSkill) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryDiagram queries the "diagram" edge of the DiagramSkill entity.
func (_m *DiagramSkill) QueryDiagram() *DiagramQuery {
	return NewDiagramSkillClient(_m.config).QueryDiagram(_m)
}

// QuerySkill queries the "skill" edge of the DiagramSkill entity.
func (_m *DiagramSkill) QuerySkill() *SkillQuery {
	return NewDiagramSkillClient(_m.config).QuerySkill(_m)
}

// Update returns a builder for updating this DiagramSkill.
// Note that you need to call DiagramSkill.Unwrap() before calling this method if this DiagramSkill
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *DiagramSkill) Update() *DiagramSkillUpdateOne {
	return NewDiagramSkillClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the DiagramSkill entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *DiagramSkill) Unwrap() *DiagramSkill {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: DiagramSkill is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *DiagramSkill) String() string {
	var builder strings.Builder
	builder.WriteString("DiagramSkill(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("diagram_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.DiagramID))
	builder.WriteString(", ")
	builder.WriteString("skill_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.SkillID))
	builder.WriteString(", ")
	builder.WriteString("linked_at=")
	builder.WriteString(_m.LinkedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// DiagramSkills is a parsable slice of DiagramSkill.
type DiagramSkills []*DiagramSkill
